$('#post-<%= @comment.post_id %> .comments-list').prepend("<%= escape_javascript  render 'comments/comment', com: @comment %>");

$('#comments-length-<%= @comment.post_id %>').html(<%= Post.find(@comment.post_id).comments.length%> + " comments");

$('#add-button-<%= @comment.post_id %> #comment-form-textarea').val('');
		